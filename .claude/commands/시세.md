새 지역 부동산 분석 스크립트 일괄 생성

지역명: $ARGUMENTS

$ARGUMENTS가 비어 있으면 "어느 지역 시세를 만들까요? (예: 강남구)" 라고 먼저 물어보고 입력받아라.
$ARGUMENTS에 지역명이 있으면 바로 아래 정보를 물어봐라.

---

아래 정보를 한 번에 모두 물어봐라. 입력받기 전까지 파일을 만들지 마라.

**필요한 정보:**
1. lawd_cd (법정동 코드 5자리, 예: 48123) — 공공데이터포털 기준
2. 원본 xlsx 파일명 (CLAUDE 폴더 안에 있는 파일, 예: `창원시 의창구.xlsx`)
3. 시세시트 탭명 (시세시트_링크추가.xlsx 안의 탭 이름, 예: `창원의창구`)
4. 수집 기간 (시작월-끝월, 예: `202101-202312`)
5. 전세 포함 여부 (매매만 / 매매+전세)

---

정보를 받으면 아래 5개 파일을 생성해라.

**공통 고정값:**
- BASE = `C:\Users\한나\OneDrive\강성업무용\바탕 화면\문서\CLAUDE`
- FILE(시세시트) = BASE + `\시세시트_링크추가.xlsx`
- service_key = `e5bf9aedc2df3e8a9d7fa0628a721e78c2fcb884ba67b2c79a10c39c37c18bff`
- 배경색 참조 = 천안동남구 탭 행9

---

## 생성 파일 1: `{지역명}_시세/config.json`

```json
{
  "service_key": "e5bf9aedc2df3e8a9d7fa0628a721e78c2fcb884ba67b2c79a10c39c37c18bff",
  "lawd_cd": "{lawd_cd}",
  "region_name": "{지역명}"
}
```

---

## 생성 파일 2: `{지역명}_시세/수집.py`

아래 패턴대로 작성. 변수만 바꾸면 됨:

```python
"""{지역명} 매매 실거래가: {시작월} ~ {끝월}"""
import sys, io, os, json, time, csv, urllib.parse, urllib.request
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

BASE = os.path.dirname(os.path.abspath(__file__))
with open(os.path.join(BASE, 'config.json'), 'r', encoding='utf-8') as f:
    cfg = json.load(f)

URL_TRADE = 'https://apis.data.go.kr/1613000/RTMSDataSvcAptTradeDev/getRTMSDataSvcAptTradeDev'
URL_RENT  = 'https://apis.data.go.kr/1613000/RTMSDataSvcAptRentDev/getRTMSDataSvcAptRentDev'  # 전세 포함 시만


def fetch_all_month(base_url, ymd):
    out = []; page = 1
    while True:
        params = {
            'serviceKey': cfg['service_key'], 'LAWD_CD': cfg['lawd_cd'], 'DEAL_YMD': ymd,
            'numOfRows': '1000', 'pageNo': str(page), '_type': 'json',
        }
        url = base_url + '?' + urllib.parse.urlencode(params, safe=':/')
        with urllib.request.urlopen(url, timeout=60) as r:
            data = json.loads(r.read().decode('utf-8'))
        body = data.get('response', {}).get('body', {})
        items = body.get('items', {})
        if not items: break
        lst = items.get('item', [])
        if isinstance(lst, dict): lst = [lst]
        out.extend(lst)
        total = int(body.get('totalCount', 0))
        if page * 1000 >= total: break
        page += 1; time.sleep(0.3)
    return out


def months_between(s, e):
    sy, sm = int(s[:4]), int(s[4:])
    ey, em = int(e[:4]), int(e[4:])
    out = []
    y, m = sy, sm
    while (y, m) <= (ey, em):
        out.append(f'{y:04d}{m:02d}')
        m += 1
        if m > 12: m = 1; y += 1
    return out


ym_list = months_between('{시작월}', '{끝월}')

# 매매 수집
FIELDS_TRADE = ['dealYear','dealMonth','dealDay','umdNm','aptNm','buildYear',
                'excluUseAr','dealAmount','floor','jibun','aptSeq']
all_trade = []
for i, ym in enumerate(ym_list, 1):
    try:
        items = fetch_all_month(URL_TRADE, ym)
        print(f'  [매매] {ym}: {len(items)}건 ({i}/{len(ym_list)})')
        all_trade.extend(items)
        time.sleep(0.3)
    except Exception as e:
        print(f'  [매매] {ym}: 에러 - {e}')

out_trade = os.path.join(BASE, '매매_원본.csv')
with open(out_trade, 'w', encoding='utf-8-sig', newline='') as f:
    w = csv.DictWriter(f, fieldnames=FIELDS_TRADE, extrasaction='ignore')
    w.writeheader()
    for it in all_trade: w.writerow(it)
print(f'매매 저장: {out_trade} ({len(all_trade)}건)')
```

전세 포함이면 매매 수집 후 동일 패턴으로 전세도 수집해서 `전세_원본.csv` 저장하는 코드 추가.
전세 필드: `['dealYear','dealMonth','dealDay','umdNm','aptNm','buildYear','excluUseAr','deposit','floor','jibun','aptSeq']`
전세 URL: `URL_RENT`

---

## 생성 파일 3: `{지역명}_전고전저.py`

- DATA_CSV = BASE + `\{지역명}_시세\매매_원본.csv`
- ws = wb['{탭명}']
- 전고 기간: 수집 시작월 ~ 수집 중간월 (자동 계산)
- 전저 기간: 수집 중간월 ~ 수집 끝월 (자동 계산)
- COL = `{'dong':1, 'name':5, 'year':6, 'area':11, 'high':21, 'low':22}` (U열, V열)
- norm_name, norm_dong, parse_amt, best_match 함수 모두 포함
- 나머지는 부산남구_전고전저.py 패턴 그대로

---

## 생성 파일 4: `{지역명}_배경색.py`

- FILE = BASE + `\시세시트_링크추가.xlsx`
- ws = wb['{탭명}']
- 단지코드 열 = D열(4번째)
- START = 5 (5행부터)
- 천안동남구 행9 참조해서 회색 패턴 추출
- 나머지는 부산남구_배경색.py 패턴 그대로

---

## 생성 파일 5: `{지역명}_하이퍼링크.py`

- FILE = BASE + `\시세시트_링크추가.xlsx`
- REF = BASE + `\{원본xlsx파일명}`
- ws = wb['{탭명}']
- dong열 = A열(1번), name열 = D열(4번)
- 네이버 부동산 URL 패턴: `https://new.land.naver.com/complexes/{코드}?ms=2AfAQG,3zprE6,17&a=APT:ABYG:JGC&b=A1:B1&e=RETAIL&h=34&i=132&l=198&ad=true`
- REF 파일에서 단지코드 매핑: 3열=읍면동, 5열=단지코드, 6열=단지명
- 나머지는 대전동구_하이퍼링크.py 패턴 그대로

---

모든 파일 생성 후: 생성된 파일 목록과 실행 순서(1→2→3→4→5) 안내해라.
